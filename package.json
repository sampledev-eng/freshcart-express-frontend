{
  "name": "freshcart-express-frontend",
  "version": "1.0.0",
  "homepage": "https://sampledev-eng.github.io/freshcart-express-frontend",
  "scripts": {
    "build": "mkdir -p dist && cp -r frontend/* dist/",
    "deploy": "gh-pages -d frontend"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
